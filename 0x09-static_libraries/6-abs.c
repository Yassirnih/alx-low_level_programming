#include "main.h"

/**
 * _abs - variable
 * @num: getting abs
 * Return: -num the negative value and num is the prositive
 */

int _abs(int num)
{

	if (num <= 0)
	{
		return (-num);
	}
	return (num);
}
